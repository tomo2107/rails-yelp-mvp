<h1>Create a new restaurant</h1>
<%= simple_form_for [@restaurant] do |f| %>
  <%= f.input :name %>
  <%= f.input :address %>
  <%= f.input :category, collection: Restaurant.categories_array, as: :select %>
  <%= f.submit "Submit review", class: "btn btn-primary" %>
<% end %>
